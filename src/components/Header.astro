---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
import DarkModeToggle from './DarkModeToggle.astro';
---

<header>
	<nav>
		<h2><a href="/">{SITE_TITLE}</a></h2>
		<button class="hamburger" aria-label="Ouvrir le menu" aria-expanded="false">
			<span></span>
			<span></span>
			<span></span>
		</button>
		<div class="internal-links">
			<HeaderLink href="/">Accueil</HeaderLink>
			<HeaderLink href="/projects">Projets</HeaderLink>
			<HeaderLink href="/experience">Expériences</HeaderLink>
			<HeaderLink href="/about">À propos</HeaderLink>
			<HeaderLink href="/cv">CV</HeaderLink>
		</div>
		<DarkModeToggle />
		<div class="social-links">
			<a href="https://www.linkedin.com/in/julien-parrot" target="_blank">
				<span class="sr-only">Se connecter sur LinkedIn</span>
				<svg viewBox="0 0 24 24" aria-hidden="true" width="32" height="32"
					><path
						fill="currentColor"
						d="M20.5 2h-17A1.5 1.5 0 002 3.5v17A1.5 1.5 0 003.5 22h17a1.5 1.5 0 001.5-1.5v-17A1.5 1.5 0 0020.5 2zM8 19H5v-9h3zM6.5 8.25A1.75 1.75 0 118.3 6.5a1.78 1.78 0 01-1.8 1.75zM19 19h-3v-4.74c0-1.42-.6-1.93-1.38-1.93A1.74 1.74 0 0013 14.19a.66.66 0 000 .14V19h-3v-9h2.9v1.3a3.11 3.11 0 012.7-1.4c1.55 0 3.36.86 3.36 3.66z"
					></path></svg
				>
			</a>
			<a href="mailto:julien.parrot22@gmail.com">
				<span class="sr-only">Envoyer un email</span>
				<svg viewBox="0 0 24 24" aria-hidden="true" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
					><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path
					><polyline points="22,6 12,13 2,6"></polyline></svg
				>
			</a>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: var(--bg-primary);
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
		min-height: 60px;
		transition: background-color 0.3s, box-shadow 0.3s;
	}

	:global(.dark) header {
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
	}
	nav {
		display: grid;
		grid-template-columns: 1fr auto auto 1fr;
		align-items: center;
		min-height: 60px;
		gap: 1rem;
	}

	nav h2 {
		justify-self: start;
	}

	.internal-links {
		justify-self: center;
		display: flex;
		gap: 0.5rem;
	}

	.social-links {
		justify-self: end;
	}
	nav a {
		padding: 1em 0.5em;
		color: var(--text-primary);
		border-bottom: 4px solid transparent;
		text-decoration: none;
		transition: color 0.3s;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	.social-links,
	.social-links a {
		display: flex;
	}

	/* Hamburger menu button */
	.hamburger {
		display: none;
		flex-direction: column;
		gap: 4px;
		background: none;
		border: none;
		cursor: pointer;
		padding: 0.5em;
		z-index: 1000;
	}

	.hamburger span {
		display: block;
		width: 25px;
		height: 3px;
		background-color: rgb(var(--black));
		transition: all 0.3s ease;
		border-radius: 2px;
	}

	.hamburger[aria-expanded="true"] span:nth-child(1) {
		transform: rotate(45deg) translate(5px, 5px);
	}

	.hamburger[aria-expanded="true"] span:nth-child(2) {
		opacity: 0;
	}

	.hamburger[aria-expanded="true"] span:nth-child(3) {
		transform: rotate(-45deg) translate(7px, -6px);
	}

	/* Mobile: 320px - 767px */
	@media (max-width: 767px) {
		header {
			min-height: 60px;
		}

		nav {
			min-height: 60px;
			grid-template-columns: 1fr auto auto;
			gap: 0.5rem;
		}

		nav h2 {
			white-space: nowrap;
			overflow: hidden;
			text-overflow: ellipsis;
			max-width: calc(100vw - 140px);
		}

		.hamburger {
			display: flex;
			min-height: 44px;
			min-width: 44px;
			align-items: center;
			justify-content: center;
			order: 3;
		}

		nav :global(#theme-toggle) {
			order: 2;
		}

		.internal-links {
			position: fixed;
			top: 60px;
			left: 0;
			right: 0;
			background: var(--bg-primary);
			flex-direction: column;
			padding: 1rem 0;
			box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
			transform: translateY(-100%);
			opacity: 0;
			visibility: hidden;
			transition: all 0.3s ease;
			z-index: 999;
		}

		.internal-links.open {
			transform: translateY(0);
			opacity: 1;
			visibility: visible;
		}

		.internal-links a {
			padding: 1em 2em;
			border-bottom: none;
			border-left: 4px solid transparent;
			width: 100%;
			display: block;
		}

		.internal-links a:hover,
		.internal-links a.active {
			border-left-color: var(--accent);
			border-bottom-color: transparent;
			background: rgb(var(--gray-light));
		}

		.social-links {
			display: none;
		}
	}
</style>

<script>
	const hamburger = document.querySelector('.hamburger');
	const nav = document.querySelector('.internal-links');

	hamburger?.addEventListener('click', () => {
		const isOpen = hamburger.getAttribute('aria-expanded') === 'true';
		hamburger.setAttribute('aria-expanded', String(!isOpen));
		nav?.classList.toggle('open');
	});

	// Close menu when clicking outside
	document.addEventListener('click', (e) => {
		if (!hamburger?.contains(e.target as Node) && !nav?.contains(e.target as Node)) {
			hamburger?.setAttribute('aria-expanded', 'false');
			nav?.classList.remove('open');
		}
	});

	// Close menu when navigating
	nav?.querySelectorAll('a').forEach(link => {
		link.addEventListener('click', () => {
			hamburger?.setAttribute('aria-expanded', 'false');
			nav?.classList.remove('open');
		});
	});
</script>
